import React from 'react';
import PlaceholderScreen from '../../components/ui/PlaceholderScreen';

export default function ExploreScreen() {
  return (
    <PlaceholderScreen
      name="Explore"
      description="Discover DJs, artists, venues, and trending content across the DECKKNOB network."
    />
  );
}
