import React from 'react';
import PlaceholderScreen from '../../components/ui/PlaceholderScreen';

export default function VenueDetailScreen() {
  return (
    <PlaceholderScreen
      name="Venue Detail"
      description="Venue info, current music, crowd level, lineup, and check-in."
    />
  );
}
